<div class="container">
<ul>

       <% @topics.each do |topic| %>
       <li class="list-group-item d-flex justify-content-between align-items-center">
          Title: <%= topic.title %>
          <%= link_to 'Destroy', admin_topic_path(topic), method: :delete %>
<% end %>
</li>
</ul>

</div>
